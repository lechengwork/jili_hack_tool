# 設目標 → 掛監看 → 有進度自己回報

> 這是 2026-09-11 抓 696 饋贈樣本時用的作法。寫下來是因為它比「人工盯著看」好用很多,
> 而且**抓到了三個人工盯絕對會漏掉的 bug**。

## 核心循環:拼圖法

```
設目標 ──▶ 針對性 spin ──▶ 資料進來 ──▶ 分析 ──▶ 先大概猜規則
                                                      │
   ◀── 修正規則 ◀── 送去分析 ◀── 反例出現 ◀── 反例監看 ◀┘
   │
   └──▶ 下一批資料自動驗證新規則 ──▶ (回到反例監看)
```

重點不是「一次猜對」,是**讓錯的猜測快速被打死**。樣本少的時候大膽猜,
然後把猜測寫成監看條件 —— 之後每一局新資料都在替你考試,錯了會當場現形。

**越拼越看到全貌。** 這次的實際軌跡:

| 題目 | v1(樣本少,先猜) | 打死它的反例 | v2 | 打死它的反例 | 定案 |
|---|---|---|---|---|---|
| 倍率規則 | 分 group 用不同起點<br>(5 局吻合) | 兩局差 1,互為鏡像 | `sum(window[2:3+N])`<br>(403 局吻合) | `group=3` 連鎖局<br>算出 7、實際 70 | `sum(window[2:len-2])`<br>**727/727 零反例** |
| `f7` 欄位 | 中獎線數 | 5 條線的局是 1 | 中獎符號種類數 | 兩種符號同時中也是 1 | 先當布林 `has_win`…<br>再被值 `3` 打死 →<br>**`win_type` 0/1/3** |

v1 在當下**不是錯的**,它是「用當時全部資料能得到的最好解釋」。
錯的是停在那裡不設監看 —— 那樣 v1 會一路帶進交付。

### 為什麼要把猜測寫成監看條件

猜完就放著,等於賭沒有反例。寫成監看條件,反例會自己跳出來找你。
`math_progress.sh` 那行 `倍率規則 727/727 局吻合 ★規則站得住★` 就是這個意思 ——
**它不是報告,是每局都在跑的回歸測試。**

同理,「封包有沒有沒解到的欄位」也是一條監看條件。那條抓到了 `f4`、`f10`、varint 三個洞。

---

## 「事情做好了,怎麼通知 AI?」

**答案是:你不通知。你讓那件事在檔案系統上留下痕跡,然後叫 AI 去盯那個痕跡。**

痕跡可以是任何東西 —— 多一個檔、log 多一行、某個數字變了、指令的結束碼。
只要 shell 看得到,就能當訊號。你照常做你的事,AI 自己會發現。

### 通知是怎麼送到 AI 那裡的

AI 呼叫 `Monitor` 工具,給它一個要跑的指令。之後:

```
watch.sh 每印一行 stdout
        │
        ▼
harness 攔截,包成一則系統訊息注入對話
        │
        ▼
AI 被「叫醒」,像收到一個 turn 一樣,可以當場動手
```

AI 那邊實際收到的原文:

```
<task-notification>
<task-id>bg9w4on4c</task-id>
<summary>Monitor event: "JILI 696 擷取:饋贈樣本 2→5、…"</summary>
<event>★饋贈樣本 2 → 3 / 5  (總局 515)
24127-151360-00350696:[0, 0, 0, 2, 0]</event>
</task-notification>
```

★**是 push 不是 poll**★ —— AI 不會一直去檢查,是事情發生時被叫醒。
所以掛上之後你可以直接去忙,不用回來貼「好了」。

### 四個要知道的細節

| 細節 | 為什麼重要 |
|---|---|
| **只有 stdout 會通知** | stderr 只進輸出檔,不會叫醒 AI。探針的錯誤要 `2>&1` 導進來 |
| **一行 = 一則訊息** | 吃對話額度。印太多會被自動停掉 —— 所以「只印會採取行動的事」是硬限制 |
| **200ms 內多行會併成一則** | 「事件 + 明細」可以分行印,會一起到達,不會打擾兩次 |
| **腳本 exit = 監看結束** | 所以「達標就退出」;`while true` 掛著會一直佔用 |

另外,通知明確標示「**NOT user input**」—— AI 不會把它當成你說了什麼或你同意了什麼。
資料到了就分析,但不能當授權。

### 三種形狀,挑對的用

| 你要的 | 用什麼 | 例子 |
|---|---|---|
| **只通知一次**(「跑完叫我」) | 背景指令 + `until` 迴圈,條件成立就自己結束 | `until grep -q "Done" build.log; do sleep 5; done` |
| **每次發生都通知,沒有終點** | Monitor + 不會結束的指令 | `tail -f app.log \| grep --line-buffered ERROR` |
| **每次都通知,但有終點** | Monitor + 會 exit 的指令 ★本文的作法★ | `watch.sh`:湊到目標局數就 `exit 0` |

★常見錯誤★:要「只通知一次」卻用了 `tail -f`。事情早就發生了,監看還掛在那裡直到逾時。

### 你實際要做的事

1. 跟 AI 說:**目標是什麼、什麼算完成、什麼算出事**
2. AI 寫探針 + 迴圈,掛上 Monitor
3. 你去做你的事(開 autoplay、跑編譯、等 CI…)
4. 有進度 / 出事 / 達標,AI 自己會知道,並且當場開始分析

---

## 解決什麼問題

要湊一批罕見樣本(饋贈觸發率不到 1%),得開 autoplay 轉幾百局。問題是:

- 人不可能一直盯著終端
- `watch -n 30 ./math_progress.sh` 會一直刷,但**你還是得自己看**,而且看到的是「現在幾局」,
  不是「有沒有出事」
- 最糟的是**沉默失敗**:側錄斷線的時候畫面看起來一切正常,你轉半小時全白費
  (這真的發生過,`keepalive` 配額那次)

## 三個零件

```
watch_state.py   一次掃描 → 吐一行機器可讀的狀態        (讀檔+算,0.08 秒)
watch.sh         每 30 秒跑一次 state,比對上次,有事才印一行;達標就 exit
Monitor 工具     把 watch.sh 掛在背景,【每一行 stdout = 一則通知送回對話】
```

關鍵是第三層:`watch.sh` 印一行,Claude 那邊就收到一則通知,可以**當場去查那一局的封包**,
不用等人回來貼注單號。

```
你開 autoplay ──▶ server 側錄 ──▶ 落檔
                                   │
                          watch_state.py 每 30s 掃
                                   │
                          watch.sh 比對,有事印一行
                                   │
                          Monitor → 通知回 session → Claude 當場分析
```

## 六個設計決策(真正的價值在這)

### 1. 沉默不等於成功

監看最容易犯的錯:只盯「好消息」。那樣的話程式當掉、側錄斷線、流程卡住,
畫面全都一樣安靜 —— **而安靜看起來就像一切正常**。

所以 `watch.sh` 除了「饋贈 +1」之外,一定會吵這三件事:

| 事件 | 為什麼一定要吵 |
|---|---|
| 側錄斷線 | 你還在轉,但資料沒進來 —— 這是最貴的失敗 |
| 倍率規則反例 | 代表我們解出來的規則是錯的,要重解 |
| 封包出現沒解到的欄位 | **adapter 可能正在掉資料**,交付出去會炸 |

掛監看前先問自己:**「如果現在整個流程掛掉,我的過濾條件會印出任何東西嗎?」**
不會的話就要放寬。

### 2. 只印「我會採取行動」的事

每行 stdout 都是一則通知,印太多會被自動停掉。所以**不要**把原始 log 接進去。
`math_progress.sh` 那種每 30 秒印 10 行的格式適合人看,**不適合當事件流**。

分界線是:看到這行我會不會做什麼?會 → 印;只是「還活著」→ 不印(改用心跳)。

### 3. 達標就 exit,不要用無界命令

`tail -f`、`while true` 這種永遠不會自己結束的命令,就算事情早就發生了也會一直掛著。
`watch.sh` 湊到目標局數就 `exit 0`,監看自然結束。

反過來說,**要「單次通知」就不該用 Monitor** —— 用會自己結束的背景指令就好。

### 4. 探針要快,而且不能有副作用

`watch_state.py` 只讀檔和計算,**不寫任何檔、不碰網路**。所以 30 秒輪詢一次完全不影響擷取。
640 局掃一次 0.08 秒。

如果探針會寫檔或發請求,輪詢就會干擾本業,那就本末倒置了。

### 5. 存活判斷要用「別人不會動到的訊號」

★這裡我踩過坑★:第一版用「樣本檔的 mtime」判斷側錄是否活著,結果
`math_migrate_keys.sh` 重寫了全部 475 個檔案,mtime 全變成當下 —— **監看永遠顯示「活著」**。

改成看 `webcap_exchanges.jsonl`(只有側錄本身會寫)才正確。

**選存活訊號的原則:挑那個「只有你要監控的東西會去動它」的檔案或計數器。**

### 6. 腳本裡 `$變數` 後面不要直接接中文

★這個也踩過★:`echo "總局 $total。"` 在 `LANG=C.UTF-8` 下會印成 `總局 ��` ——
bash 把 `。` 的 UTF-8 位元組當成變數名的一部分,`$total` 整個變空。

```bash
echo "總局 $total。"     # ✗ 643 被吃掉
echo "總局 ${total}。"   # ✓
```

中文訊息又是我們的日常,所以**變數後面接中文一律加大括號**。
掃全檔:`grep -P '\$[A-Za-z_]\w*[^\x00-\x7F]' *.sh`

## 這次實際抓到什麼

湊樣本本來是目標,但監看真正的價值在附帶抓到的東西:

| 抓到 | 人工盯看得出來嗎 |
|---|---|
| 饋贈樣本 2 → 5 局 | 可以,但要一直貼注單號 |
| **`BoardDetail.f4`** 連鎖 Ex Nudge 的第二段次數 | ✗ 完全看不出來 |
| **`BoardDetail.f10`** 饋贈加權的轉輪符號 | ✗ 完全看不出來 |
| **bytes 其實是 packed varint**(500 被讀成 `244,3`) | ✗ 完全看不出來 |

後面三個都是**罕見狀態才會出現的欄位** —— 平常 600 局跑下來一切正常,一撞到就掉資料。
如果沒有「未知欄位」這條監控,這些會一路帶進交付給機率的檔案裡。

## 搬到別的任務

換掉 `watch_state.py` 裡「算什麼」的部分就好,骨架不用動:

```python
# 1. 目標指標(你在等的東西)
# 2. 壞消息指標(至少一個:流程死掉的訊號)
# 3. 存活指標(只有被監控對象會更新的東西)
print(f"{目標}|{壞消息}|{存活秒數}")
```

`watch.sh` 的五段(要的東西 / 壞消息 / 斷線 / 心跳 / 達標退出)照抄改條件即可。

## 跑法

```bash
cd /Users/dyson/Downloads/jili
./watch.sh                    # 自己在終端跑
GIFT_TARGET=10 ./watch.sh     # 改目標
INTERVAL=60 ./watch.sh        # 改輪詢間隔
```

給 Claude 掛的話,直接說「設目標 X,掛監看,有進度再回報」就行。
